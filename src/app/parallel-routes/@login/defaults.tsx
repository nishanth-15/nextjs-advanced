import Page from "./page";

{
  /*
  This page is rendered by NextJS when we navigate to the '/parallel-routes/{sub-route}' route and if {sub-route} is not defined in this slot(i.e login slot) instead of rendering a 404 page
*/
}
export default function Default() {
  return <Page />;
}
